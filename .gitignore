# dependencies (none currently — zero-dependency Node)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Secrets — NEVER commit. ANTHROPIC_API_KEY is read from the environment only.
.env
.env.*
*.local
secrets.json

# Notes:
# data/processed/*.json and dashboard/index.html are generated, but we DO commit them
# so a fresh clone is immediately viewable. Re-run `node src/refresh.mjs` to regenerate.

# Transient verifier / source-check reports (regenerated each run; not dashboard data)
data/processed/source-check.json
data/processed/brief-verify.json
data/processed/brief-audit.json
