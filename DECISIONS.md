# Urban Update — Decision Log

> The canonical record of product & architecture decisions. **Every collaborator (human or LLM) reads this first.** Append new decisions; don't rewrite history. Flag any contradiction with an entry here before acting.

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| D1 | 2026-08-03 | Build Urban Update as a **market-intelligence product** for NY real estate. | Core loop: monitor → detect regime change → identify who's affected → explain with evidence. | Active |
| D2 | 2026-08-03 | **Portfolio project**, not a startup chasing paying users. | Goal is to expand the founder's AI portfolio by showing AI replicate institutional research. Success = a credible, working artifact — not signed pilot users. | Active |
| D3 | 2026-08-03 | **Mimic institutional research** (CBRE / JLL / Cushman / Marcus & Millichap style) using **public data that is easy to obtain and easy for AI to analyze.** | This is the primary design constraint. Favors structured public APIs (NYC Open Data, ACRIS, FRED) over messy/private data (insurance quotes, private loan terms, sentiment). | Active |
| D4 | 2026-08-03 | Artifact is a **dashboard** (each panel carries an AI-written, cited interpretation). | Supersedes the earlier "report-first" call — a dashboard is more portfolio-impressive and demonstrates the AI live. The evidence-first loop is preserved via per-panel citations. | Active — supersedes earlier report-first note in SPEC |
| D5 | 2026-08-03 | Audience = **industry professionals broadly** (owners, developers, investors, brokers). | Institutional "state of the market" research is consumed across roles. Earlier owner-vs-developer debate dissolves under the portfolio goal. | Active — supersedes owner/operator-only framing |
| D6 | 2026-08-03 | **Build one feed end-to-end first: Development × NYC.** Expand to Transactions (ACRIS) + Capital/macro (FRED) after the pattern is proven. | Development data (DOB/DCP) is the most public, countable, and AI-parseable. Prove the loop on the cleanest feed. | Active |
| D7 | 2026-08-03 | Stack = **Node.js (zero-dependency) pipeline + self-contained static HTML dashboard.** | Node 24 + curl already present; no Python. Founder is an AI builder, not a traditional engineer — keep it simple, portable, reviewable. | Active |
| D8 | 2026-08-03 | Primary source for Development feed = **DOB NOW: Build – Job Application Filings** (`w9ak-ipjd`). Legacy `ic3t-wcy2` is historical-only (New Buildings dead-end at 2020). | Verified live: fresh through 2026-08-01, has `filing_date`, `job_type`, `proposed_dwelling_units`, `borough`, `nta`, `bbl`; ~10yr history. See [SOURCES](SOURCES.md). | Active |
| D9 | 2026-08-03 | Regime detection v1 = **transparent rule** (latest complete month vs. trailing-12-month average + YoY), not ML. | Principle: transparent rules before opaque models; every number must be explainable and cited. | Active |
| D10 | 2026-08-03 | **Neighborhood (NTA) drill-down:** click a borough → modal ranks its neighborhoods with New Building filings, each row verifiable. | User wants to click into a borough and see neighborhood-area detail. NTA field is clean (~1% null). | Active |
| D11 | 2026-08-03 | At neighborhood grain, **suppress noisy signals**: only flag Elevated/Cooling when 12-mo avg ≥2 or year-ago ≥3; show the 12-month average (not a %) as context; show YoY% only when year-ago base ≥3. | Small denominators produce absurd %s (e.g. +1400% off a base of 1) that read as hype and kill credibility. | Active |
| D12 | 2026-08-03 | Source verified & **GitHub live**: DOB `official` provenance, daily updates, figures re-checked against source. Repo `github.com/cajiang/urban-update` (public). | User asked to verify reputable sourcing + freshness and to save work to GitHub against usage limits. | Active |
| D13 | 2026-08-03 | **Rejected ACRIS Master** (`bnx9-e6tj`) for the transactions feed — recent borough coverage is broken (Brooklyn ~77 deeds/mo vs thousands in reality; Staten Island absent). Built on **DOF Rolling (`usep-8jbt`) + Annualized (`w2pb-icbu`) Calendar Sales** instead, split at the year boundary. | User mandate: trusted, monitorable, complete sources only. Verified all 5 boroughs present & balanced; both DOF `official`. See [SOURCES](SOURCES.md). | Active |
| D14 | 2026-08-03 | **Dashboard refactored to a tabbed multi-feed layout** (Development · Transactions), each with its own regime call, KPIs, borough grid, notable list, and provenance panel. | Second feed needs its own space; tabs scale as FRED etc. are added. Single self-contained HTML retained. | Active |
| D15 | 2026-08-03 | Transactions **lag handling**: sales accrue for weeks, so the latest complete month = source update-month − 2 (currently May 2026). Reported in-dashboard. | Prevents presenting a partial month as a regime shift. | Active |
| D16 | 2026-08-03 | **FRED rejected as next step; chose to deepen instead.** Added neighborhood drill-down to the Transactions feed (DOF `neighborhood` field), mirroring Development — both feeds now symmetric. | FRED is commodity, non-local, non-differentiated data available everywhere; deepening local depth is higher-impact and leverages our edge. FRED demoted to optional context strip, later. | Active |
| D17 | 2026-08-03 | **Cross-Signal view** (3rd tab): joins Development (supply) + Transactions (demand) by **property ZIP** (DOB `postcode` ⋈ DOF `zip_code`) and flags divergence — oversupply-watch (supply↑/demand↓) vs tightening (demand↑/supply↓). | The "so what" synthesis; the actual product promise (identify who/what is affected). Uses existing feeds, sets up the LLM layer. | Active |
| D18 | 2026-08-03 | Cross-Signal uses **trailing-quarter YoY** off enforced minimum bases (≥8 filings/≥45 sales this qtr, ≥4/≥25 a year ago); reference = the latest quarter both feeds fully cover (transactions lag → May 2026). | Monthly per-ZIP filings are too sparse (gave +500% off a base of 1). Quarterly smoothing + base gates = credible, institutional framing. | Active |
| D19 | 2026-08-03 | Use DOB **`postcode`** (property ZIP), NOT `zip` (applicant's address ZIP), for any geographic join. | `zip` surfaced Nassau County ZIPs (applicant offices); `postcode` is the property location. | Active |
| D20 | 2026-08-03 | **Risk feed (4th tab)** — the differentiator. Built on the fastest, most-reliable sources: **HPD violations + 311 + ECB (all daily/weekday, official)**. Risk Pressure = 3×(Class C) + 1×(Class B), trailing quarter, YoY. Severity-weighted, transparent. | User: risk is a huge selling point ("know first"). Diligence showed building-risk data updates daily — faster than sales; deferred slow/gap sources (liens biannual, flood=census-tract geometry, foreclosure not on Open Data). | Active |
| D21 | 2026-08-03 | **Risk folded into Cross-Signal** too (HPD Class C by ZIP overlaid on supply/demand). | User wanted both a Risk tab AND integration. Now supply + demand + risk read together — the triple synthesis almost nobody does. | Active |
| D22 | 2026-08-03 | Risk noise gate: ZIP/borough needs ≥60 weighted pts now + ≥40 a year ago to flag Rising/Falling. | Same small-base lesson (Financial District showed +3160% off ~5). | Active |
| D25 | 2026-08-03 | **LLM interpretation layer (Brief tab).** `src/narrate.mjs` calls Claude (`claude-opus-5`) at refresh time with a grounded evidence packet from all 5 feeds; writes `narratives.json`; dashboard renders it as the landing "Brief" tab. | The synthesis "so what" layer — the crucial piece the user held for last. Generated once per daily refresh, served static to all viewers (matches the product's cadence). | Active |
| D26 | 2026-08-03 | Narration uses **raw `fetch` to the Messages API**, not the Anthropic SDK; **structured JSON output**; strict grounding (model may only use provided numbers, fact vs. inference separated). Needs `ANTHROPIC_API_KEY`; skips gracefully if absent. | Keeps the zero-dependency-Node design (no npm/node_modules). Grounding + structured output prevent fabrication. User supplies the key. | Active |
| D23 | 2026-08-03 | **Risk phase 2 = Local Law 97** (5th tab). Validated as the single highest-impact risk top players won't miss (carbon penalties: $268/ton, in effect now, tighten 2030). Estimates per-building exposure from LL84 benchmarking (`5zyy-y8am`). | Distress (foreclosure) sourcing unreliable; facades/flood lower-impact. LL97 is the big quantifiable exposure smaller owners can't compute. | Active |
| D24 | 2026-08-03 | LL97 model = screening estimate: emissions (reported location-based GHG) vs limit (primary occupancy-group intensity × GFA), penalty = $268 × tons over. Result ~$759M/yr 2024 basis, $2.3B 2030, 18%/73% over — matches published estimates. | Disclosed approximation (primary type vs full multi-occupancy). Derive borough from BBL (text field null); drop intensity>0.05 tCO2e/sqft outliers. | Active |
| D27 | 2026-08-03 | **Capital-conditions feed (7th tab, "Capital") — reverses D16's FRED rejection, as a *lens* not a strip.** Pulls 10Y Treasury, 30Y mortgage, SOFR, Fed Funds from **FRED via the keyless CSV endpoint** (no API key; keeps zero-dependency design). The differentiation D16 lacked: rates presented as context on our local feeds — cost of capital vs. NYC housing **demand** (recorded sales). `src/capital.mjs` → `capital.json`. Hero = "2021 low-rate boom vs. now": mortgage 2.96%→6.36%, 10Y 1.44%→4.24%, **sales −26%**. | D16 correctly rejected a *bare commodity rates strip*; a lens on our differentiated local data is on-thesis (the master variable everyone watches, lowest-cost reliable data). Wired into the AI Brief + refresh chain. | Active — reverses D16 with its reasoning intact |
| D28 | 2026-08-03 | **Capital lens is anchored on DEMAND (sales), NOT development filings.** Filings correlation with rates is spurious (DOB NOW migration → filings only complete ~2021; 421-a deadlines drive them). Correlation computed on **YoY changes (detrended)**: 30Y-mortgage↔sales r=−0.21; reported as association, not causation. | Evidence standard: refused to ship a spurious +0.76 filings correlation. Sales have clean 2016+ data and are the genuinely rate-sensitive channel. | Active |
| D29 | 2026-08-03 | **API-key security policy.** `ANTHROPIC_API_KEY` is read from the environment only — never committed, never in chat/memory, never received by Claude. User runs the key-dependent step (`refresh`) in their own shell (ephemeral env var or secret-manager fetch). `.gitignore` covers `.env`/secrets. | User's standing security requirement across projects; a key in a public repo, transcript, or persistent memory is a leak. | Active |
| D30 | 2026-08-05 | **Next feed = Demand & Affordability (8th tab).** Census ACS 5-year (income, rent, cost burden, tenure, population) by **ZCTA (ZIP)** — joins to existing ZIP-keyed feeds — combined with our DOF median price + FRED mortgage rate to compute an affordability engine (income required vs. local income; share who can afford) and a price-vs-income-vs-rent **divergence monitor**. Fold into Cross-Signal. | A top-tier operator's review made the gap unmistakable: we cover "what's happening" (supply/sales/risk) but not "who lives here and can they pay." Affordability is the #1 add, purely public/verifiable, and deepens Transactions + Cross-Signal. | **Active — BUILT & VERIFIED 2026-08-05** (8th tab shipped; folded into Cross-Signal + narrate packet; see D33) |
| D31 | 2026-08-05 | **Census API requires a free key** (`CENSUS_API_KEY`); keyless access is retired and NYC Open Data's ACS profiles are non-tabular (Excel, unusable via our pipeline). Key is free + low-sensitivity (rate token, not billing). User stored it as a **user env var** ("simple path") so build/verify tooling can read it; still env-only, never in chat/repo/memory (extends D29). Read into an already-running process via `[Environment]::GetEnvironmentVariable('CENSUS_API_KEY','User')`. | Only credible source of ACS-by-geography; a free rate token is acceptable for tooling to use directly, unlike the paid Anthropic billing credential. | Active |
| D32 | 2026-08-05 | **Feature roadmap from the top-tier interview: build vs. defer vs. skip.** BUILD: Demand/Affordability (D30), Development upgrade (DCP Housing DB + PLUTO: filed→permitted→completed, net units vs stock), LL97 neighborhood-concentration reframe, property-tax/assessment. DEFER: construction feasibility, infrastructure/public-investment, facility-externality impact study, quality-of-life trajectory. SKIP (no clean free source or curatorial → would break the evidence standard): cap rates, office occupancy, construction/insurance cost, retail vacancy/concessions, broad regulatory-headline tracking, employer/capital-flow tracking. | An operator gave a 12-category wishlist; matching all of it would sprawl into a generic platform and force unsourced/soft numbers. Discipline: only feasible + verifiable + deepens-existing. | Active |
| D33 | 2026-08-05 | **Demand & Affordability build specifics.** (a) **Vintages: ACS5 2024 (current) + 2020 (prior)** — both 2020-Census ZCTA boundaries (consistent-geography change window); pre-2020 ACS uses 2010 ZCTAs + different geography call, so it's excluded. (b) **Price basis by grain:** DOF transacted median at borough/citywide (reliable), **ACS median home value at ZIP** (per-ZIP DOF medians too thin). (c) **Affordability engine assumptions (disclosed in-dashboard):** 20% down / 80% LTV, 30-yr fixed at current FRED rate, 30% housing DTI, **P&I only** (excludes taxes/insurance/maintenance → income-required is a *floor*); "share who can afford" interpolates B19001 (uniform within brackets, Pareto α=2.0 above $200k) → labeled an *estimate*. (d) **Divergence monitor thresholds** (cumulative growth-gap pts, 2020→2024): affordability-stress rent−income ≥5, yield-compression value−rent ≥10, improving-fundamentals income−value ≥10, else supported/mixed. (e) **Geography for borough/citywide = county/place ACS** (authoritative medians, not averaged ZIPs). | Every figure re-verified against the live ACS payload before shipping (evidence standard). The ownership "gap" is largest in low-income, majority-renter ZIPs (denominator effect) — surfaced honestly and paired with renter cost burden, not framed as "everyone there is buying." | Active |
| D34 | 2026-08-05 | **Automated daily refresh + verification-gated auto-publish.** Replace the manual "run `.\run.ps1`" habit with a **check-on-open launcher** (`open.ps1`, at most once/day): `check-sources.mjs` (freshness gate — only refresh when a source is actually newer than our last pull, saving Anthropic tokens) → `refresh.mjs` → **two Brief verifiers** → open dashboard. Verifiers: (1) `verify-brief.mjs` — deterministic, confirms every figure in the Brief traces to a value in the evidence packet (precision-aware tolerance; catches ungrounded/hallucinated numbers); (2) `pressure-test.mjs` — an adversarial `claude-opus-5` audit flagging unsupported claims, unwarranted causation, overstated certainty, or investment advice. **Auto-publish (commit+push) is GATED**: `open.ps1 -Publish` pushes only when BOTH verifiers pass; default is local-only. A static HTML artifact can't self-refresh or expose a viewer-triggered rebuild, and viewer-triggered rebuilds would spend the owner's tokens — so refresh runs on the owner's machine, not from a viewer's browser. `narrate.mjs` refactored to export `buildEvidence`/`loadFeeds` (single source of truth for the packet) with a direct-run guard so importing it for the verifiers has no side effects. | User: stop running the command daily; auto-publish is the goal but only behind verifiers/agents that pressure-test the Brief so nothing wrong ships. The evidence standard is the moat — publishing should be *earned* by passing verification. The paid Anthropic key stays DPAPI-encrypted and is never handled by the coding assistant (extends D29/D31); the classifier also blocks the assistant from reading it. | Active |

## Principles (from the product brief, adopted)
- **Evidence before eloquence** — no claim ships without a verifiable primary source.
- **Facts vs. inference stay separate** — label confirmed facts vs. interpretation.
- **Primary sources first** — original filing/dataset over trade press.
- **Transparency over false precision** — explainable scores, no black boxes.
- **Build narrowly before expanding** — one feed, one region, proven, then widen.
